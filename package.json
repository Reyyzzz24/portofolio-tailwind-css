{
  "name": "my-portofolio",
  "version": "1.0.0",
  "description": "Portofolio RY",
  "main": "scripts/main.js",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./style.css -o ./output.css --watch",
    "build": "npx @tailwindcss/cli -i ./style.css -o ./output.css --minify"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0"
  }
}